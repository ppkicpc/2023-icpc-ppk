#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ull = unsigned long long;
using ip = pair<int, int>;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cout << fixed;
    cout.precision(3);
    int a, b;
    cin >> a >> b;
    cout << (float)b / a << '\n';
}